# Source assets

Photographic sources for the build-time grid conversion. Not committed
publicly if you'd rather not; the build skips any job whose source is missing.

- `lily.png` — the flower for the cold open and the closing scene.
- `portrait.png` and `portrait-mask.png` — the headshot and a subject mask
  (white = keep, black = drop) so the background does not render.
